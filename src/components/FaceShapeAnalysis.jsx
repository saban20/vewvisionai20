import React from 'react';
import { Card, CardContent, Typography } from '@mui/material';

const FaceShapeAnalysis = () => (
  <Card className="hologram-card" sx={{ mb: 4 }}>
    <CardContent>
      <Typography variant="h6" className="hologram">Face Shape</Typography>
      <Typography>Analyzing face shape... (Placeholder)</Typography>
    </CardContent>
  </Card>
);

export default FaceShapeAnalysis; 